import java.lang.annotation.ElementType;
import java.util.Iterator;

public class PositionalList {
    //Il modo più naturale per implementare una lista posizionale è una lista doppiamente concatenata
    // Una lista utilizza un iteratore, astrazione dello scorrimento degli elementi uno per volta.

    Iterator<ElementType> iter = collection.iterator();
    while (iter.hasNext()){
        ElementType var=iterator.next();
    }
}
